Azure Databricks ETLTest Automation Framework
PySpark + PyTest | CSV/TXT → Raw → Operational → Curated
A practical learning and interview reference for building reusable data-quality and reconciliation tests inside Azure Databricks.
Prepared for: Lingam D Reddy
1. Framework Goal
The purpose of this framework is to validate data at every stage of a Databricks ETL pipeline. PySpark performs scalable data comparisons and transformations, while PyTest provides repeatable test execution and assertions that mark validations as PASS or FAIL.
CSV / TXT    |    vSOURCE FILES    |  Source-to-Raw validations    vRAW LAYER    |  Raw-to-Operational validations    vOPERATIONAL LAYER    |  Operational-to-Curated validations    vCURATED LAYER    |    vPYTEST RESULTS: PASS / FAIL
2. Validation Strategy by Layer
Stage
Core validations
Source files
File/path accessibility, not empty, delimiter/columns, schema/format, record count
Source → Raw
Count reconciliation, missing/unexpected keys, schema/columns, ingestion completeness
Raw layer
Mandatory NULLs, duplicate primary keys, data types, allowed values
Raw → Operational
Transformations, filters, datatype conversions, rejected records, key reconciliation
Operational layer
NULLs, duplicates, business rules, referential/key integrity
Operational → Curated
Aggregations, derived metrics, transformations, incremental-load logic
Curated layer
Final business metrics, NULLs, duplicates, allowed values, data quality
3. Recommended Project Structure
etl_test_framework/├── config/│   └── validation_config.py├── framework/│   ├── data_reader.py│   ├── validations.py│   └── reconciliation.py├── tests/│   ├── conftest.py│   ├── test_source_raw.py│   ├── test_raw_operational.py│   └── test_operational_curated.py└── requirements.txt
4. Configuration
Keep environment paths, tables, keys and mandatory columns outside the test logic. Replace the sample catalog and storage locations with your project values.
CUSTOMER_CONFIG = {    "source": {        "type": "csv",        "path": "abfss://landing@storage.dfs.core.windows.net/customer/customers.csv",        "header": True,        "delimiter": ","    },    "raw": {"table": "catalog.raw.customer"},    "operational": {"table": "catalog.operational.customer"},    "curated": {"table": "catalog.curated.customer"},    "primary_key": ["customer_id"],    "mandatory_columns": ["customer_id", "first_name", "last_name"]}
5. Generic PySpark Data Reader
from pyspark.sql import DataFrameclass DataReader:    def __init__(self, spark):        self.spark = spark    def read_csv(self, path, delimiter=",", header=True) -> DataFrame:        return (self.spark.read                .option("header", str(header).lower())                .option("delimiter", delimiter)                .option("inferSchema", "true")                .csv(path))    def read_txt(self, path, delimiter="|") -> DataFrame:        return (self.spark.read                .option("header", "true")                .option("delimiter", delimiter)                .option("inferSchema", "true")                .csv(path))    def read_table(self, table_name) -> DataFrame:        return self.spark.table(table_name)    def read_sql(self, query) -> DataFrame:        return self.spark.sql(query)
6. Reusable Validation Library
from pyspark.sql import functions as Fclass DataValidator:    @staticmethod    def validate_record_count(source_df, target_df):        source_count = source_df.count()        target_count = target_df.count()        assert source_count == target_count, (            f"Record count mismatch: Source={source_count}, Target={target_count}"        )    @staticmethod    def validate_not_empty(df):        assert df.limit(1).count() > 0, "Dataset is empty"    @staticmethod    def validate_nulls(df, columns):        for column in columns:            null_count = df.filter(F.col(column).isNull()).count()            assert null_count == 0, f"{column} contains {null_count} NULL values"    @staticmethod    def validate_duplicates(df, key_columns):        duplicates = (df.groupBy(*key_columns).count()                        .filter(F.col("count") > 1))        duplicate_count = duplicates.count()        assert duplicate_count == 0, (            f"Found {duplicate_count} duplicate keys for {key_columns}"        )    @staticmethod    def validate_columns(df, expected_columns):        missing = set(expected_columns) - set(df.columns)        assert not missing, f"Missing columns: {missing}"    @staticmethod    def validate_allowed_values(df, column, allowed_values):        invalid_count = df.filter(~F.col(column).isin(allowed_values)).count()        assert invalid_count == 0, (            f"{column} contains {invalid_count} invalid rows"        )
7. Key Reconciliation
Record counts alone can pass even when the wrong records are present. A left-anti join identifies source keys missing from the target and target keys that are unexpected.
class Reconciliation:    @staticmethod    def validate_key_reconciliation(source_df, target_df, key_columns):        source_keys = source_df.select(*key_columns).distinct()        target_keys = target_df.select(*key_columns).distinct()        missing = source_keys.join(target_keys, key_columns, "left_anti")        unexpected = target_keys.join(source_keys, key_columns, "left_anti")        missing_count = missing.count()        unexpected_count = unexpected.count()        assert missing_count == 0, (            f"{missing_count} source keys are missing from target"        )        assert unexpected_count == 0, (            f"{unexpected_count} unexpected keys found in target"        )
8. PyTest Spark Fixture
When the tests execute on a Databricks cluster, expose the active SparkSession through a fixture.
# tests/conftest.pyimport pytestfrom pyspark.sql import SparkSession@pytest.fixture(scope="session")def spark():    return SparkSession.builder.getOrCreate()
9. Source → Raw Test Suite
import pytestfrom framework.data_reader import DataReaderfrom framework.validations import DataValidatorfrom framework.reconciliation import Reconciliationfrom config.validation_config import CUSTOMER_CONFIG@pytest.fixture(scope="module")def source_raw(spark):    reader = DataReader(spark)    source = reader.read_csv(        CUSTOMER_CONFIG["source"]["path"],        CUSTOMER_CONFIG["source"]["delimiter"],        CUSTOMER_CONFIG["source"]["header"]    )    raw = reader.read_table(CUSTOMER_CONFIG["raw"]["table"] )    return source, rawdef test_source_not_empty(source_raw):    source, raw = source_raw    DataValidator.validate_not_empty(source)def test_source_raw_count(source_raw):    source, raw = source_raw    DataValidator.validate_record_count(source, raw)def test_raw_mandatory_nulls(source_raw):    source, raw = source_raw    DataValidator.validate_nulls(raw, CUSTOMER_CONFIG["mandatory_columns"])def test_raw_duplicate_keys(source_raw):    source, raw = source_raw    DataValidator.validate_duplicates(raw, CUSTOMER_CONFIG["primary_key"])def test_source_raw_key_reconciliation(source_raw):    source, raw = source_raw    Reconciliation.validate_key_reconciliation(        source, raw, CUSTOMER_CONFIG["primary_key"]    )
10. Raw → Operational Transformation Tests
For transformation testing, calculate the expected value from Raw using the documented business rule and compare it with the actual value in Operational.
from pyspark.sql import functions as Fdef test_full_name_transformation(raw_df, operational_df):    expected = (raw_df        .withColumn("expected_full_name",            F.concat_ws(" ", F.col("first_name"), F.col("last_name")))        .select("customer_id", "expected_full_name"))    actual = operational_df.select("customer_id", "full_name")    comparison = expected.join(actual, "customer_id", "inner")    mismatches = comparison.filter(        ~F.col("expected_full_name").eqNullSafe(F.col("full_name"))    )    assert mismatches.count() == 0, (        f"Found {mismatches.count()} full_name transformation failures"    )def test_status_transformation(raw_df, operational_df):    expected = raw_df.select(        "customer_id", F.upper("status").alias("expected_status"))    actual = operational_df.select(        "customer_id", F.col("status").alias("actual_status"))    mismatches = expected.join(actual, "customer_id").filter(        ~F.col("expected_status").eqNullSafe(F.col("actual_status"))    )    assert mismatches.count() == 0
11. Operational → Curated Aggregation Test
from pyspark.sql import functions as Fdef test_customer_total_amount(operational_df, curated_df):    expected = (operational_df.groupBy("customer_id")        .agg(F.sum("amount").alias("expected_amount")))    actual = curated_df.select(        "customer_id", F.col("total_amount").alias("actual_amount"))    mismatches = expected.join(actual, "customer_id", "full_outer").filter(        ~F.col("expected_amount").eqNullSafe(F.col("actual_amount"))    )    assert mismatches.count() == 0, (        f"Found {mismatches.count()} aggregate mismatches"    )
12. SQL-Based Business Rule Validation
If you already have Databricks SQL validation queries, keep using them. A generic helper can enforce the rule that a query returns zero invalid records.
def validate_query_returns_zero(spark, query):    invalid_df = spark.sql(query)    invalid_count = invalid_df.count()    assert invalid_count == 0, (        f"Validation query returned {invalid_count} invalid rows"    )# Examplequery = """SELECT *FROM catalog.curated.customer_salesWHERE total_amount < 0"""validate_query_returns_zero(spark, query)
13. How an Assertion Fits
source_count = source_df.count()target_count = target_df.count()assert source_count == target_count, "Count mismatch"
The Spark code calculates the actual result. The assertion is the final checkpoint. If the condition is True, the test passes; if False, PyTest reports a failed test and displays the message.
14. Recommended Test Case Matrix
ID
Area
Validation
Expected result
TC001
Source
File/data not empty
At least one record
TC002
Source → Raw
Record count
Counts reconcile after documented filters
TC003
Raw
Mandatory NULLs
Zero NULLs in mandatory columns
TC004
Raw
Duplicate primary key
Zero duplicate keys
TC005
Source → Raw
Key reconciliation
No missing/unexpected keys
TC006
Raw → Operational
Transformation
Expected equals actual
TC007
Operational
Allowed status
Only approved values
TC008
Operational → Curated
Aggregation
Expected metric equals curated metric
TC009
Curated
Negative amount
Zero invalid rows
TC010
All layers
Schema/columns
Expected columns available
15. Running Tests in Databricks
Use Python test files named test_*.py or *_test.py and test functions prefixed with test.
Attach tests to an appropriate Databricks compute environment when Spark/table access is required.
Run tests from Databricks workspace test tooling where available, or execute pytest in your project workflow.
Keep reusable framework code separate from individual pipeline test suites.
pytest -v tests/
16. Framework Improvements for a Real Project
Make validations configuration-driven using JSON/YAML or control tables.
Capture failed record samples to an audit Delta table instead of only returning a count.
Add batch_id/load_date filters so validations compare the correct incremental load.
Add datatype and schema comparison from table metadata.
Add referential-integrity tests between fact and dimension/business tables.
Write execution metadata: run_id, pipeline, layer, test_case, expected, actual, result, timestamp.
Integrate test execution into your Databricks job or CI/CD workflow.
For very large datasets, avoid collect(); compare with Spark joins, aggregates and hashes at distributed scale.
17. Interview Explanation
A concise answer you can use:
“In our Azure Databricks ETL architecture, source CSV/TXT data flows through Raw, Operational and Curated layers. I use PySpark to read and reconcile distributed datasets and PyTest to organize automated validations. The framework separates configuration, data readers, reusable validations, reconciliation logic and layer-specific tests. I validate counts, mandatory NULLs, duplicate keys, schema, source-to-target keys, transformation rules and curated aggregations. For business rules already expressed in SQL, the framework can execute Databricks SQL and assert that invalid-record queries return zero rows.”
18. Study Checklist
Understand Spark DataFrames: select, filter, groupBy, join and withColumn.
Understand inner, left, full_outer and especially left_anti joins.
Understand PyTest fixtures and assertions.
Be able to explain why record-count validation alone is insufficient.
Practice calculating expected transformations yourself and comparing with target data.
Practice NULL-safe comparisons using eqNullSafe for transformed fields.
Know how to restrict validations to a batch/load date for incremental ETL.
Be ready to explain Raw → Operational → Curated validations in an interview.
19. Reference
Databricks documentation describes unit testing for notebooks with Python/PyTest and Python test discovery/execution in the workspace. Review the current documentation for the exact options enabled in your workspace and cloud deployment:
• https://docs.databricks.com/aws/en/notebooks/testing
• https://docs.databricks.com/aws/en/files/python-unit-tests
• https://docs.databricks.com/aws/en/dev-tools/vscode-ext/pytest
20. Sequential Execution Flow: What Runs First and Why
This section explains the framework in the exact order you should imagine it executing. Read it from Step 1 to Step 10 whenever the class, object, fixture, DataFrame, validation, or assertion feels confusing.
Step 1 - Databricks provides the Spark session
The test starts on Databricks compute. The SparkSession named spark is the entry point used to read files, tables, and execute Spark SQL.
spark
Step 2 - PyTest provides spark through the fixture
The session fixture returns the active SparkSession. Tests and data-loading fixtures can request spark as an argument.
@pytest.fixture(scope="session")def spark():    return SparkSession.builder.getOrCreate()
Step 3 - Create the DataReader object
We create one object from the DataReader class and give it Spark. Creating the object automatically runs __init__.
reader = DataReader(spark)
Step 4 - __init__ stores Spark inside the object
Python automatically calls __init__(self, spark). self represents the new reader object. self.spark = spark stores the SparkSession so every reader method can reuse it.
def __init__(self, spark):    self.spark = spark
Step 5 - Read the source file
We ask the reader object to execute its read_csv method. The method uses self.spark.read and returns a Spark DataFrame. That returned DataFrame is stored in source_df.
source_df = reader.read_csv(source_path)
Step 6 - Read the Raw table
The same reader object already remembers Spark, so we do not pass spark again. read_table returns the Raw table as another Spark DataFrame.
raw_df = reader.read_table("catalog.raw.customer")
Step 7 - Data is now ready for comparison
At this point source_df and raw_df are Spark DataFrames. Validation code works on these DataFrames using count, filter, groupBy, joins, and column expressions.
source_count = source_df.count()raw_count = raw_df.count()
Step 8 - Execute a validation
A reusable validation method receives the two DataFrames and calculates the actual values that need to be compared.
DataValidator.validate_record_count(source_df, raw_df)
Step 9 - Assertion decides PASS or FAIL
Inside the validation, assert checks the expected condition. Equal counts mean PASS. Different counts raise AssertionError and PyTest marks the test as failed.
assert source_count == raw_count, (    f"Count mismatch: Source={source_count}, Raw={raw_count}")
Step 10 - Repeat for the remaining Source-to-Raw tests
The same loaded DataFrames can be validated for mandatory NULLs, duplicate keys, expected columns, and missing/unexpected keys. This is why the reader and validation utilities are reusable.
validate_nulls(raw_df, mandatory_columns)validate_duplicates(raw_df, primary_key)validate_key_reconciliation(source_df, raw_df, primary_key)
20.1 One Complete Source-to-Raw Execution Example
# 1. Spark already exists in Databricks# 2. Create a DataReader object and store Spark in itreader = DataReader(spark)# 3. Read source CSV -> source_dfsource_df = reader.read_csv(source_path)# 4. Read Raw table -> raw_dfraw_df = reader.read_table("catalog.raw.customer")# 5. Execute validationsDataValidator.validate_not_empty(source_df)DataValidator.validate_record_count(source_df, raw_df)DataValidator.validate_nulls(raw_df, ["customer_id", "first_name", "last_name"])DataValidator.validate_duplicates(raw_df, ["customer_id"])Reconciliation.validate_key_reconciliation(source_df, raw_df, ["customer_id"])
20.2 Mental Model
CLASS: DataReader = blueprint for reading data
OBJECT: reader = actual DataReader created with Spark
METHODS: read_csv/read_table/read_sql = actions reader can perform
DATAFRAMES: source_df/raw_df = data returned by the methods
VALIDATOR: calculates a test condition from the DataFrames
ASSERT: final checkpoint that turns the condition into PASS or FAIL
20.3 Execution in One Picture
Databricks spark      |      vreader = DataReader(spark)      |      v__init__ stores self.spark      |      +-------------------+      |                   |      v                   vread_csv()           read_table()      |                   |      v                   v source_df              raw_df      |                   |      +---------+---------+                |                v          validations                |                v             assert          /           \       PASS           FAIL
23. Final Visual Blueprint: Framework Flow and Folder Map
Use these final pages as your quick-reference picture while building the project. Blue sections represent reusable framework code, green represents tests, orange represents configuration, and purple represents reporting/output.
A. END-TO-END AUTOMATION FLOW
1  RUN PYTESTpytest -v tests/ --alluredir=allure-results
▼
2  PYTEST DISCOVERS TESTStest_source_raw.py • test_raw_operational.py • test_operational_curated.py
▼
3  FIXTURES PREPARE SPARK & DATAconftest.py → SparkSession → DataReader → DataFrames
▼
4  READ CONFIGURATIONpaths • table names • keys • mandatory columns
▼
5  EXECUTE VALIDATIONScount • null • duplicate • transformation • reconciliation
▼
6  ASSERTIONS DECIDE RESULTexpected == actual → PASS / FAIL
▼
7  ALLURE RESULTSpytest writes execution result files
8  ALLURE HTML REPORTsummary • failed tests • steps • attachments • trends
B. DATA-LAYER VALIDATION FLOW
CSV / TXT
RAW
OPERATIONAL
CURATED
SOURCE
INGESTION
TRANSFORM
BUSINESS
       Source→Raw              Raw→Operational              Operational→Curated
   count • keys • schema       trim • cast • CASE • filters       joins • aggregates • windows
C. EXACT FOLDERS TO CREATE
📁 etl_test_framework/   project root
  └─ requirements.txt
  └─ pytest.ini  (optional)
  └─ README.md  (recommended)
📁 config/   WHAT / WHERE to test
  └─ validation_config.py  → source paths, tables, primary keys, mandatory columns
  └─ environment_config.py  → dev/test environment values (optional)
📁 framework/   HOW to read and validate
  └─ data_reader.py  → DataReader: read_csv, read_txt, read_table, read_sql
  └─ validations.py  → DataValidator: count, null, duplicate, columns, allowed values
  └─ reconciliation.py  → missing/unexpected key comparison
  └─ transformation_validator.py  → expected-vs-actual transformation comparison (later)
  └─ reporting.py  → reusable Allure attachments/helpers (later)
📁 tests/   WHAT test cases to execute
  └─ conftest.py  → Spark and shared DataFrame fixtures
  └─ test_source_raw.py  → source→raw validations
  └─ test_raw_operational.py  → transformations, filters, data types, dedup
  └─ test_operational_curated.py  → joins, aggregations, business metrics, windows
📁 allure-results/   generated raw reporting output
  └─ JSON/result files created by pytest + allure-pytest
  └─ Do not hand-code test logic here
📁 allure-report/   generated HTML report
  └─ HTML dashboard generated from allure-results
  └─ PASS/FAIL summary, steps, attachments and history when configured
D. PROJECT TREE AT A GLANCE
etl_test_framework/├── config/│   ├── validation_config.py│   └── environment_config.py        # optional├── framework/│   ├── data_reader.py│   ├── validations.py│   ├── reconciliation.py│   ├── transformation_validator.py  # add later│   └── reporting.py                 # Allure helpers, add later├── tests/│   ├── conftest.py│   ├── test_source_raw.py│   ├── test_raw_operational.py│   └── test_operational_curated.py├── allure-results/                  # generated├── allure-report/                   # generated├── requirements.txt├── pytest.ini                       # optional└── README.md                        # recommended
23.1 Beginner Build Order
STEP
CREATE / BUILD
WHY
1
Notebook practice
Read CSV + Raw table, count/null/duplicate
2
framework/data_reader.py
Move reusable reading logic
3
framework/validations.py
Move reusable validations
4
config/
Move paths, keys and columns out of test code
5
tests/conftest.py
Create Spark/shared data fixtures
6
tests/test_source_raw.py
First PyTest suite
7
Raw→Operational
Add transformation tests
8
Operational→Curated
Add aggregates and business tests
9
Allure
Generate results and HTML report
23.2 One-Line Memory Map
CONFIG tells WHERE  →  READER gets DATA  →  TEST calls VALIDATOR  →  ASSERT gives RESULT  →  ALLURE shows REPORT
24. Senior-Level Real-Time Transformation Testing (7+ Years)
For a 7+ years ETL testing interview, do not stop at trim, uppercase, count and NULL checks. Be prepared to explain how you validate complex business transformations, change processing, historical dimensions, joins, aggregations, deduplication, late-arriving data, schema changes, idempotency and large-scale reconciliation in Databricks.
SOURCE / RAW    |    +--> cleanse + standardize    +--> deduplicate + sequence    +--> lookup/enrich + multi-table joins    +--> CDC / insert-update-delete logic    +--> SCD history    +--> aggregate / window metrics    +--> incremental / late-arriving handling    |    vOPERATIONAL / CURATED    |    vEXPECTED vs ACTUAL + INVARIANTS + RECONCILIATION    |    vPASS / FAIL + FAILURE SAMPLES + ALLURE
24.1 Multi-Column Conditional Transformation
Real rules often depend on several inputs, not one column. Example: derive order_status from payment status, cancellation flag and shipped timestamp.
expected = raw_df.withColumn(    "expected_order_status",    F.when(F.col("is_cancelled") == True, "CANCELLED")     .when((F.col("payment_status") == "PAID") & F.col("shipped_ts").isNotNull(), "SHIPPED")     .when(F.col("payment_status") == "PAID", "READY")     .otherwise("PENDING"))
24.2 Complex Financial Derivation with Rounding and NULL Rules
Example: net_amount = quantity × unit_price - discount + tax. Use explicit decimal handling and rounding rules so expected values do not differ because of floating-point behavior.
expected = operational_df.withColumn(    "expected_net_amount",    F.round(        (F.col("quantity") * F.col("unit_price"))        - F.coalesce(F.col("discount_amount"), F.lit(0))        + F.coalesce(F.col("tax_amount"), F.lit(0)),        2    ))
24.3 Multi-Table Enrichment and Referential Integrity
A fact pipeline may enrich transactions from customer, product and geography dimensions. Validate not only the resulting values but also join cardinality and unmatched foreign keys.
expected = (sales_df    .join(customer_dim.select("customer_id", "segment"), "customer_id", "left")    .join(product_dim.select("product_id", "category"), "product_id", "left")    .join(geo_dim.select("geo_id", "region"), "geo_id", "left"))unmatched_customer = sales_df.join(customer_dim, "customer_id", "left_anti")assert unmatched_customer.count() == 0
Senior interview point: a join can accidentally multiply rows. Reconcile both expected row count/cardinality and business keys after the join, not just selected columns.
24.4 Latest-Record Deduplication with Tie-Breaking
Real feeds can contain repeated business keys. Define a deterministic ordering such as event timestamp, ingestion timestamp and sequence number.
w = Window.partitionBy("customer_id").orderBy(    F.col("event_ts").desc(),    F.col("ingestion_ts").desc(),    F.col("sequence_no").desc())expected = (raw_df    .withColumn("rn", F.row_number().over(w))    .filter(F.col("rn") == 1)    .drop("rn"))
24.5 CDC / MERGE Validation: Inserts, Updates and Deletes
For Delta-style upserts, classify source changes and validate each operation separately. A senior answer should distinguish INSERT, UPDATE and DELETE populations, then verify target state after processing.
insert_keys = changed_df.filter(F.col("op") == "I").select("customer_id")update_keys = changed_df.filter(F.col("op") == "U").select("customer_id")delete_keys = changed_df.filter(F.col("op") == "D").select("customer_id")# Example post-load invariantsmissing_inserts = insert_keys.join(target_df.select("customer_id"), "customer_id", "left_anti")missing_updates = update_keys.join(target_df.select("customer_id"), "customer_id", "left_anti")still_present_deletes = delete_keys.join(target_df.select("customer_id"), "customer_id", "inner")assert missing_inserts.count() == 0assert missing_updates.count() == 0assert still_present_deletes.count() == 0
24.6 SCD Type 1 Validation
Type 1 keeps only the current value. Test that changed attributes are overwritten, unchanged attributes remain correct, no duplicate business key is introduced, and no historical row is retained when history is not required.
duplicate_current = target_dim.groupBy("customer_id").count().filter(F.col("count") > 1)assert duplicate_current.count() == 0
24.7 SCD Type 2: Full Historical Invariants
For Type 2, senior-level testing should validate the complete temporal behavior, not only the current flag.
Exactly one current row per business key.
Changed attributes create a new version when required.
Previous current row is expired correctly.
effective_from <= effective_to for expired rows.
No overlapping effective-date ranges for the same business key.
Unchanged source rows should not create unnecessary new versions.
Surrogate-key behavior is consistent with the model.
w = Window.partitionBy("customer_id").orderBy("effective_from")check = (target_dim    .withColumn("next_effective_from", F.lead("effective_from").over(w))    .filter(        F.col("effective_to").isNotNull() &        F.col("next_effective_from").isNotNull() &        (F.col("effective_to") >= F.col("next_effective_from"))    ))assert check.count() == 0, "Overlapping SCD2 effective periods found"
24.8 Late-Arriving Dimension / Unknown Member Scenario
A fact can arrive before its dimension row. Depending on the design, the pipeline may use an Unknown surrogate key and subsequently re-key the fact. Test both the initial fallback behavior and the correction after the dimension becomes available.
unknown_facts = fact_df.filter(F.col("customer_sk") == -1)# Reconcile only records for which the dimension truly was unavailable at processing time.
24.9 Incremental Load and Watermark Boundary Testing
Incremental tests should explicitly cover the watermark boundaries: before watermark, exactly at watermark, after watermark, duplicate re-delivery, and late records. Always define the comparable batch before count or key reconciliation.
expected_delta = source_df.filter(    (F.col("modified_ts") > previous_watermark) &    (F.col("modified_ts") <= current_watermark))target_delta = target_df.filter(F.col("batch_id") == current_batch_id)Reconciliation.validate_key_reconciliation(    expected_delta, target_delta, ["customer_id"])
24.10 Idempotency / Re-Run Testing
A mature ETL pipeline should behave predictably when the same batch is re-run. For an idempotent target, rerunning the same input should not create duplicate facts or duplicate current dimension versions.
before = target_df.select("business_key").count()# Re-run same batch through the pipeline in the controlled test environment.after = spark.table(target_table).select("business_key").count()assert before == after, "Re-run changed target row count unexpectedly"
24.11 Schema Evolution and Contract Validation
Validate expected additions, removals, type changes and nullable changes deliberately. Do not treat every schema difference as equivalent. Identify whether the change is backward compatible or breaks downstream assumptions.
expected_types = {    "customer_id": "bigint",    "amount": "decimal(18,2)",    "status": "string"}actual_types = dict(target_df.dtypes)for col_name, expected_type in expected_types.items():    assert actual_types.get(col_name) == expected_type
24.12 Complex Aggregations: Multiple Measures and Grain
When validating Curated aggregates, first state the target grain. Then calculate all measures at exactly that grain. Incorrect grain is a common source of false reconciliation.
expected = (operational_df    .groupBy("business_date", "region", "product_category")    .agg(        F.sum("net_amount").alias("expected_sales"),        F.countDistinct("order_id").alias("expected_orders"),        F.countDistinct("customer_id").alias("expected_customers")    ))
24.13 Window Transformations: Ranking, Running Totals and Previous Value
w_customer = Window.partitionBy("customer_id").orderBy("transaction_ts")expected = (operational_df    .withColumn("previous_amount", F.lag("net_amount").over(w_customer))    .withColumn("next_amount", F.lead("net_amount").over(w_customer)))rank_w = Window.partitionBy("region").orderBy(F.col("sales_amount").desc())ranked = sales_summary.withColumn("sales_rank", F.dense_rank().over(rank_w))
24.14 Pivot / Unpivot and Reshaping
Some Curated datasets reshape rows into reporting columns. Validate the output grain, generated column set, totals before versus after reshaping, and NULL/default handling.
expected = (operational_df    .groupBy("customer_id")    .pivot("quarter", ["Q1", "Q2", "Q3", "Q4"])    .agg(F.sum("net_amount")))
24.15 Semi-Structured JSON Parsing and Explode
Real feeds can contain JSON or arrays inside CSV/TXT/bronze data. Define a schema, parse the JSON, explode arrays where required, then validate both parent-level and child-level cardinality.
parsed = raw_df.withColumn(    "payload_struct",    F.from_json(F.col("payload"), payload_schema))items = parsed.withColumn(    "item",    F.explode_outer(F.col("payload_struct.items")))
24.16 Hash-Based Wide-Row Reconciliation
For very wide tables, a deterministic row hash can help identify changed rows without writing dozens of column comparisons. Normalize NULLs and data formats consistently before hashing, and use key-level investigation for any mismatched hashes.
compare_cols = ["name", "status", "country", "amount"]normalized = [F.coalesce(F.col(c).cast("string"), F.lit("<NULL>")) for c in compare_cols]expected = raw_df.withColumn(    "row_hash",    F.sha2(F.concat_ws("||", *normalized), 256))
24.17 Negative and Rejected-Record Reconciliation
Senior testers should validate bad-record handling too. If 100 source records enter and 3 are rejected by documented rules, then 97 valid target rows plus 3 auditable rejects can be the correct outcome.
assert source_count == accepted_count + rejected_count# Also validate each rejected row has an expected reason code / rule identifier.
24.18 Senior-Level Failure Diagnostics
Do not design tests that only return a number. Capture enough diagnostics to debug quickly while keeping large data distributed.
Mismatch count and validation name.
Small failure sample, for example limited business keys, attached to Allure.
Source/target table and batch identifiers.
Expected versus actual values for sampled failures.
Rule name, run_id, processing layer and timestamp.
Avoid collect() on the complete failed dataset; limit diagnostic samples.
24.19 Interview Scenario Matrix for 7+ Years
Scenario A - Counts match but data is wrong
Explain bidirectional key reconciliation and column/business-rule reconciliation.
Scenario B - Operational count is lower than Raw
Do not call it a failure immediately. Recalculate the documented filter/reject population and reconcile accepted + rejected records.
Scenario C - Join doubles the target count
Investigate join cardinality, duplicate dimension keys and many-to-many relationships.
Scenario D - Incremental load misses updates
Inspect watermark boundary, CDC sequencing, batch filter, timezone handling and update keys.
Scenario E - SCD2 creates duplicates
Validate one-current-row invariant, sequence ordering, unchanged-row behavior and effective-period overlap.
Scenario F - Retry creates duplicates
Test idempotency and merge/business-key behavior for the same batch.
Scenario G - Curated metric is wrong
Recompute independently from Operational at the exact target grain and reconcile components before final aggregate.
24.20 What a 7+ Years Candidate Should Say
“I do not limit ETL automation to record counts. I validate the expected business population, keys, transformations, join cardinality, rejected records, incremental boundaries, CDC operations, SCD history, aggregates and schema contracts. In Databricks I calculate expected datasets independently with PySpark/SQL, compare them at the correct business grain, use distributed anti-joins or hashes for scalable reconciliation, capture diagnostic failure samples, and execute the suites through PyTest with Allure reporting. I also validate restart/idempotency and batch-specific behavior because production ETL failures are often caused by state and sequencing, not only column transformations.”
24.21 Senior Learning Priority
FOUNDATIONcount / null / duplicate / schema / keys        |        vTRANSFORMATIONtrim / cast / CASE / filters / dates / financial rules        |        vRELATIONALjoins / cardinality / lookup / referential integrity        |        vSTATEFUL ETLCDC / MERGE / SCD1 / SCD2 / incremental / watermark / rerun        |        vANALYTICALaggregations / windows / ranking / pivots / complex grain        |        vPRODUCTION QUALITYschema evolution / rejects / diagnostics / scalability / Allure